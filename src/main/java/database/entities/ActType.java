package database.entities;

/**
 * Enumerator that is used on the client side to link activities to activity types.
 * The ordinals are linked to the ids of the activityTypes in the database/JSON file.
 */
public enum ActType { vegetarian_meal,local_produce,bike,
    public_transport,lower_temperature,solar_panel }
